# React and Angular declare wide, sometimes conflicting peer ranges. We resolve
# them deliberately in each package rather than letting install fail the repo.
strict-peer-dependencies=false
auto-install-peers=true

# Workspace packages must resolve to the local source, never the registry.
link-workspace-packages=deep
prefer-workspace-packages=true
save-workspace-protocol=rolling
